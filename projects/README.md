This directory is used for per-project runtime state, such as timeline memory databases.

- Each project gets its own SQLite DB: `projects/<project_id>/memory.db`
- This directory is gitignored by default.

